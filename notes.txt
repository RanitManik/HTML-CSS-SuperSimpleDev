css selector:it selects a class
css entity:
CSS specificity: The selector that is most specific has more priority
text elements:
void elements: elements like "link", "input" does not required any closing tag  
There are 3 types of elements in html -
  1.Block elements : The element will start on a new line and occupy
    the full width available. And you can set width and
    height values
  2.Inline elements :The element doesn't start on a new line and only
    occupy just the width it requires. You can't set the
    width or height
  3.Inline Block elements : lt's formatted just like the inline element, where it
    doesn't start on a new line. BUT, you can set width
    and height values
div by default is an block element 
div is nothing but container which contain the group of elements
block element = take up their line in their container (div)
images overflow 
to set the image size to its container size use the property " width : 100%  "
to align use vertical-align property
font-stack lets us support a backup font